/** Tiny leaf module so exporters can label modes without importing blueprint.js. */
export const MODE_LABELS = {
  'single-file': 'Single-file HTML app: one index.html, no build, no server.',
  'full-stack': 'Full-stack application: server + database + client, staged.',
};
